<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<ns2:sqlResource xmlns:ns2="http://restsql.org/schema">
    <query>
		SELECT 
        products.id AS ID,
        products.mpn,
        products.name,  
        products.description, 
        products.price,
        measure_unit.code AS measureUnit,
        products.updated_at AS lastUpdate
        FROM products, measure_unit
        WHERE measure_unit.id = products.measure_unit
	</query>
    <metadata>
        <database default="mysql_rest_sql_docker"/>
        <table name="products" role="Parent" rowSetAlias="products"/>
        <table name="measure_unit" role="ParentExtension"/>
    </metadata>
</ns2:sqlResource>
